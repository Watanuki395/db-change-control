CREATE TABLE users_tb (
  user_id INT PRIMARY KEY,         -- ID único de usuario
  username VARCHAR(255) NOT NULL,   -- Nombre de usuario
  first_name VARCHAR(255) NOT NULL -- Primer nombre del usuario
);
